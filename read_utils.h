#ifndef READ_UTILS_H_INCLUDED
#define READ_UTILS_H_INCLUDED

int safeReadInteger();

int safeReadLength();

int* type_array(int length);

int* typeSafeDownToUpArray(int length);

bool* typeMatrix(int length);

#endif // READ_UTILS_H_INCLUDED
